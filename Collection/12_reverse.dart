void main() {
  var numbers = [1, 2, 3, 4, 5];
  print(numbers.reversed.toList()); // Output: [5, 4, 3, 2, 1]
  var strings = ['a,', 'b', 'c', 'd'];
  print(strings.reversed.toList()); // Output: [d, c, b, a,]
}
